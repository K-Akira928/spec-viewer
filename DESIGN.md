# SpecViewer — デザイン仕様書

設計書を閲覧するためのサイト「SpecViewer」のデザイン仕様・決定事項。
設計検討を通じて確定した内容を逐次反映する。

---

## 1. 概要

| 項目 | 内容 |
|---|---|
| サイト名 | SpecViewer |
| 目的 | 設計書（仕様書）を閲覧・検索・ナビゲートする |
| 技術スタック | VitePress 2.0 + Tailwind CSS v4 (+ Vue) |
| ソース | `docs/` を `srcDir` とする Markdown（git 管理＝版管理） |
| 想定利用者 | 社内メンバー |
| 想定規模 | 文書数百件程度 |

## 2. スコープ

**閲覧専用（Viewer）。** 読む・探す・ナビゲートするのみを担う。

- 編集・新規作成 → GitHub 等の外部で行う
- 承認・公開ワークフロー → 持たない
- 認証・権限制御 → 持たない（静的サイト）

### 2.1 除外したもの（決定済）

- 改訂履歴（revision history）— 2026-06-14 削除決定
- 新規作成・編集画面
- 承認・公開ワークフロー
- ロール・権限制御

## 3. 情報アーキテクチャ（IA）

**第1階層 = 3 セクション**（グローバルナビに直接対応）:

| セクション | 扱う内容 |
|---|---|
| **API** | API仕様書・エンドポイント定義・スキーマ |
| **画面** | 画面設計書・UI仕様・画面遷移 |
| **ガイド** | 執筆ガイドライン・テンプレート・規約 |

> 旧候補（アーキテクチャ / データ / セキュリティ / 運用）は、この3分類の**下位（サブカテゴリ or タグ）**に回す。

### 3.1 配下構造（決定: ② サブカテゴリツリー）

`セクション → サブカテゴリ → 文書` の **2階層ツリー**。
文書規模は数百件程度を想定するため、フラットではなくツリーを採用。
サブカテゴリは **ディレクトリ構造**で表現する（例: `docs/api/auth/*.md`）。

## 4. データモデル（frontmatter）

設計書は本文 Markdown と同ファイルの frontmatter にメタ情報を集約する。
**一意性はファイルパス（URL）で表す** — `id` の採番は行わない。

```yaml
---
title: 認証・認可アーキテクチャ
description: OAuth 2.1 / OIDC を用いた…   # 一覧・OGP で使用
category: API              # 第1階層: API | 画面 | ガイド（ディレクトリから自動取得も可）
status: 承認済み            # ドラフト | レビュー中 | 承認済み | 廃止
reviewers: [佐藤, 鈴木]
created: 2026-01-10
updated: 2026-06-12        # 最終更新日（改訂履歴は持たない）
dependsOn:                 # 依存している文書（方向的・パス配列）
  - ../../guide/architecture-overview.md
related:                   # 関連文書（参照用・パス配列）
  - ./signup.md
  - ../../ops/runbook.md
---
```

> **注:** `id` / `author` / `tags` は廃止 — 識別子はファイルパス。`revision` / 改訂履歴も削除済み。
> `dependsOn` = この文書が依存する文書（方向的）。`related` = 関連文書（参照用）。ともに相対パスで指定。
> サブカテゴリは frontmatter ではなく **ディレクトリパス** から導出する（§3.1）。

## 5. デザイン方向

**採用: 01 Reference**（クリーンなライト系ドキュメント）

- タイポグラフィ: Inter
- アクセント: indigo（`#4f46e5`）
- ニュートラル: slate
- 角丸: `rounded-xl`、余白多め、細いボーダー

> 初期検討では公文書風（RFC）・ターミナル風・ミニマル風も比較検討したが、可読性と汎用性から Reference を採用（比較案のモックアップは削除済み）。

## 6. ヘッダー仕様（確定）

```
┌────────────────────────────────────────────────────────────┐
│ ▣ SpecViewer   API   画面   ガイド   [設計書を検索… ⌘K]  ◐ ↗ │
│  brand→home         nav(3)            inline search    theme repo│
└────────────────────────────────────────────────────────────┘
```

| ゾーン | 内容 |
|---|---|
| **左** | ブランド `▣ SpecViewer`（クリック → ホーム＝一覧） |
| **中央** | ナビ: `API` / `画面` / `ガイド` |
| **右** | インライン検索バー（⌘K でフォーカス）／ ダークモード切替 ／ GitHub↗ |

**挙動:** スティッキー ／ 現在地セクションのアクティブ表示 ／ モバイルは中央ナビをハンバーガーに折たたみ

## 7. サイドバー仕様（確定）

**コンテキスト型セクションツリー（2階層）** — 現セクションの中身のみを表示。

| 項目 | 内容 |
|---|---|
| 種別 | 現セクション（API/画面/ガイド）の中身のみ。ヘッダーでセクション切替 |
| 構造 | セクション → サブカテゴリ（折たたみ ▾）→ 文書 |
| 配置 | section ページ・doc ページのみ。**ホームには置かない**（フルワイド） |
| 役割 | 構造ナビ専用。**フィルタ・ソートは持たない**（→ コンテンツ上部ツールバー） |
| 現在地 | 現在文書をハイライト |
| モバイル | ドロワー（ハンバーガーで開閉） |
| **全体折りたたみ** | サイドバー上部 « で隠す ／ `Ctrl/Cmd+B` で切替。隠した際は左端に » 復元ボタン。状態は保持（localStorage） |

```
┌────────────────────────┐
│ API                    │ ← 現セクション（sticky）
│────────────────────────│
│ ▾ 認証                  │
│   認証・認可アーキテクチャ │ ← 現在（ハイライト）
│   トークン仕様            │
│ ▾ 注文                  │
│   注文 API 仕様           │
│   注文状態遷移            │
│ ▸ 決済                  │ ← 折たたみ
│────────────────────────│
│ 画面 ↗   ガイド ↗       │ ← 他セクション（任意）
└────────────────────────┘
```

**サブカテゴリの取得:** ディレクトリ構造から自動構築（`docs/api/auth/`、`docs/api/order/` …）。VitePress がツリーを生成。

## 8. ナビゲーションの役割分散

| 目的地 | 導線 |
|---|---|
| 全文書一覧 | ブランドクリック → ホーム |
| カテゴリ別（セクション内） | 各ページのサイドバー（§7 セクションツリー） |
| 最近の更新 | ホーム内のセクション |
| セクション（API/画面/ガイド） | グローバルナビ |

## 9. 採用デフォルト

| 項目 | 内容 |
|---|---|
| 検索 | VitePress 組み込み local search（minisearch） |
| ダークモード | あり（ヘッダーで切替） |
| モバイル | 対応（サイドバー＝ドロワー、目次＝折たたみ） |

## 10. 画面一覧

| 画面 | 状態 | サイドバー | 備考 |
|---|---|---|---|
| ホーム / ランディング | モックアップ（最新） | **なし**（フルワイド） | ヒーロー(バッジ＋タイトル＋コピー・CTAなし)＋セクションカード(API/画面/ガイド)＋最近の更新。統計なし |
| セクション一覧 | モックアップ（最新） | あり | フィルタツールバー(ステータス/サブカテゴリ/並び順)＋文書リスト(行型) |
| 詳細 / 閲覧 | モックアップ（最新） | あり | 本文＋目次＋メタ |
| 検索 | モックアップ（最新） | —（モーダル） | パレット型（⌘K／インラインバー起動）。ハイライト・セクション絞り込み・キーボード操作 |
| 依存関係マップ | モックアップ（最新） | — | グラフ（ノード・リンク）・文書フォーキス。左セレクタ／中央グラフ／右詳細。`dependsOn` を可視化 |

## 11. モックアップ

`design-mockups/` に格納（Tailwind CDN + Google Fonts を読込むため表示はオンライン環境が必要）。

| ファイル | 内容 | 反映状況 |
|---|---|---|
| `index.html` | ギャラリー（全案への導線） | — |
| `01-reference.html` | ホーム/ランディング ★採用方向 | 最新反映済（ヘッダー確定版・サイドバー削除でフルワイド） |
| `05-reference-reading.html` | 読む画面 ★採用方向 | サインアップ API（POST /signup）を表示。API設計フォーマット準拠・ヘッダー・ツリーサイドバー・全体折たたみ |
| `06-section-list.html` | セクション一覧 ★採用方向 | フィルタツールバー＋文書リスト(行型)・サイドバー(全体折たたみ) |
| `07-search.html` | 検索パレット ★採用方向 | モーダル型。ハイライト・セクション絞り込み・キーボード操作 |
| `08-deps.html` | 依存関係マップ ★採用方向 | 文書フォーカス型の依存グラフ（ノード・リンク）。セレクタ＋詳細パネル |

## 12. 未決定項目

主要な設計要素は確定済み。残るは実装段階の詳細:

- [ ] ステータス値の最終定義（ドラフト/レビュー中/承認済み/廃止）
- [ ] ダークモードの配色トークン
- [ ] VitePress 実装（`config.mts` / `Layout.vue` / `index.ts` / `docs/` 構造）

## 13. API設計フォーマット

「API」セクションの設計書（原則 1文書＝1エンドポイント）は [`docs/guide/api-format.md`](docs/guide/api-format.md) の標準フォーマットに従う。規範サンプル: [`docs/api/auth/signup.md`](docs/api/auth/signup.md)（`POST /signup`）。

- 構成: **API情報 / リクエスト / レスポンス / 副作用 / シナリオ(BDD/Gherkin)**
- **OpenAPI は使わず** Markdown のみ（人間可読・軽量）
- エラー形式: `{ code, message }`（コードは `UPPER_SNAKE_CASE`）
- **ページネーションは標準化せず案件毎**
- 副作用は主操作と成否を分離（最終的整合性）・idempotency key で冪等

---

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-06-14 | 初版。スコープ（閲覧専用）・IA（API/画面/ガイド）・ヘッダー仕様を確定。改訂履歴を削除。 |
| 2026-06-14 | 配下構造（②サブカテゴリツリー）・想定規模（数百件）・サイドバー仕様を確定。ホームはサイドバーなし。 |
| 2026-06-14 | サイドバー全体の折りたたみ（« ボタン／`Ctrl/Cmd+B`）を追加。 |
| 2026-06-14 | 05 モックアップから改訂履歴を削除。決定内容への反映を完了。 |
| 2026-06-14 | ホーム画面構成を確定：ヒーロー(CTA)＋セクションカード＋最近の更新（統計は削除）。01に反映。 |
| 2026-06-14 | セクション一覧画面（06）を作成：フィルタツールバー＋文書リスト(行型)。 |
| 2026-06-14 | 検索パレット（07）を作成：モーダル型・ハイライト・セクション絞り込み・キーボード操作。主要4画面が揃い設計確定。 |
| 2026-06-14 | API設計フォーマット（`docs/guide/api-format.md`）を策定。規範サンプルとして `POST /signup`（`docs/api/auth/signup.md`）を追加。 |
| 2026-06-14 | `id` 採番を廃止。識別子はファイルパス（URL）で表す。`related` はパス参照に統一。 |
| 2026-06-14 | `author`/`tags` を廃止。`dependsOn`（依存文書）を追加 — `related`（関連文書）とともにパスで運用。 |
| 2026-06-14 | 依存関係マップ（08）を作成：文書フォーカス型の依存グラフ（ノード・リンク可視化）。 |
| 2026-06-14 | 05 を実API文書（サインアップ API / POST /signup）の内容に再構成し、モックと文書の乖離を解消。06 も整合。 |
| 2026-06-14 | 05 シナリオを Gherkin 全7シナリオ全文掲載（折り返し表示で見切れ解消）＋副作用をカード化して読みやすさ改善。 |
| 2026-06-14 | 副作用セクションを4カード（内部/外部/共通）に統一。プロジェクト全体の絵文字を除去（✅/❌→必須/任意、🏠📂📄🔍🕸 等）。 |
| 2026-06-14 | 05 副作用を固定スキーマ化（方式=enumタグ・項目固定＝属人化しない機械的入力）。シナリオを Gherkin 構造化レンダリング（Feature/Background/Scenario・ステップ色分け）に変更。 |
| 2026-06-14 | 05 副作用を `###` 見出し（内部状態／外部への副作用／共通の失敗時の扱い）で区切る構成に変更。カードの種別タグ（内部/外部/共通）を廃止し、signup.md と表示構造を整合。TOC にもサブ見出しを反映。副作用カードは2列→1列に変更。 |
| 2026-06-14 | 副作用カードの項目を 内容/経路/失敗時 の3項目に固定（対象は見出し/カードタイトル）。執筆時の項目取捨選択を排除し画一化。api-format.md §1.4・signup.md も同構造へ更新。 |
| 2026-06-14 | 共通ポリシー（共通の失敗時の扱い）を 整合性/主操作失敗時/リトライ/冪等性 の4項目に固定し画一化。api-format.md §1.4 に規約化。 |
| 2026-06-14 | 非採用モック（02 RFC／03 Console／04 Mono）を削除。ギャラリー（index.html）を採用5画面（01/05/06/07/08）に集約。 |
